{
  // extends the whole project config
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // states types as cypress to avoid intersection with jest
    "types": ["cypress"]
  },
  "include": ["../node_modules/cypress", "./**/*.ts"]
}
